### What is an element?

An element is a plain object describing a component instance or DOM node and its desired properties.

### What is `<App />`?

`<App />` is a React element that represents the App component. It is a JSX syntax
that gets transpiled to React.createElement(App) by Babel.
